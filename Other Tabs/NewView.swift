import SwiftUI

/// Placeholder for the New tab.
struct NewView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("New tab coming soon")
                .foregroundColor(.white)
                .font(.title2)
        }
    }
}
